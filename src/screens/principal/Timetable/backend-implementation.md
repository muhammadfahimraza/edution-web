# Backend implementation — H14 Timetable overview

## Overview
Read-only weekly timetable by class section for principal oversight.

## Proposed API
`GET /schools/{slug}/timetable?section={sectionId}`
