# Backend implementation — H13 Points & merit

## Overview
School-wide points rankings (class/grade/school scope) and merit award timeline.

## Proposed API
`GET /schools/{slug}/principal/points?scope=&termId=`
`GET /schools/{slug}/principal/merit-events?limit=`
